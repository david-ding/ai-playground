# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: stories.spec.ts >> renders fileupload--error
- Location: packages/web/vr-tests/stories.spec.ts:17:3

# Error details

```
Error: expect(locator).toHaveScreenshot(expected) failed

Locator: locator('#storybook-root')
  2516 pixels (ratio 0.03 of all image pixels) are different.

  Snapshot: fileupload--error.png

Call log:
  - Expect "toHaveScreenshot(fileupload--error.png)" with timeout 5000ms
    - verifying given screenshot expectation
  - waiting for locator('#storybook-root')
    - locator resolved to <div id="storybook-root">…</div>
  - taking element screenshot
    - disabled all CSS animations
  - waiting for fonts to load...
  - fonts loaded
  - attempting scroll into view action
    - waiting for element to be stable
  - 2516 pixels (ratio 0.03 of all image pixels) are different.
  - waiting 100ms before taking screenshot
  - waiting for locator('#storybook-root')
    - locator resolved to <div id="storybook-root">…</div>
  - taking element screenshot
    - disabled all CSS animations
  - waiting for fonts to load...
  - fonts loaded
  - attempting scroll into view action
    - waiting for element to be stable
  - captured a stable screenshot
  - 2516 pixels (ratio 0.03 of all image pixels) are different.

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - generic [ref=e4] [cursor=pointer]:
    - button "file upload"
    - paragraph [ref=e5]: Drag & drop a file here, or click to select
  - generic [ref=e6]:
    - paragraph [ref=e7]: "Upload failed: Failed to get upload URL: Service Unavailable"
    - button "Try again" [ref=e8]
```

# Test source

```ts
  1  | import { readFileSync } from 'node:fs';
  2  | import { expect, test, openWithFonts, waitForFonts } from './fixtures';
  3  | 
  4  | const BRAND = JSON.parse(readFileSync(new URL('./brands.json', import.meta.url), 'utf8'))[0];
  5  | type StoryIndexEntry = { id: string; type?: string };
  6  | const index = JSON.parse(
  7  |   readFileSync(
  8  |     new URL(`../storybook-static/${BRAND}/index.json`, import.meta.url),
  9  |     'utf8',
  10 |   ),
  11 | ) as { entries: Record<string, StoryIndexEntry> };
  12 | const storyIds = Object.values(index.entries)
  13 |   .filter((entry) => entry.type === 'story')
  14 |   .map((entry) => entry.id);
  15 | 
  16 | for (const storyId of storyIds) {
  17 |   test(`renders ${storyId}`, async ({ page }, testInfo) => {
  18 |     const brand = testInfo.project.name;
  19 |     await openWithFonts(page, `/storybook/${brand}/iframe.html?id=${storyId}`);
  20 |     await page.locator('#storybook-root').waitFor({ state: 'attached' });
  21 |     await waitForFonts(page);
> 22 |     await expect(page.locator('#storybook-root')).toHaveScreenshot(`${storyId}.png`, {
     |                                                   ^ Error: expect(locator).toHaveScreenshot(expected) failed
  23 |       animations: 'disabled',
  24 |       maxDiffPixelRatio: 0.001,
  25 |     });
  26 |   });
  27 | }
  28 | 
```