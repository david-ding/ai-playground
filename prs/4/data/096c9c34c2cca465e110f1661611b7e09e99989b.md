# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pages.spec.ts >> home page mobile
- Location: packages/web/vr-tests/pages.spec.ts:9:3

# Error details

```
Error: expect(page).toHaveScreenshot(expected) failed

  864 pixels (ratio 0.01 of all image pixels) are different.

  Snapshot: home-mobile.png

Call log:
  - Expect "toHaveScreenshot(home-mobile.png)" with timeout 5000ms
    - verifying given screenshot expectation
  - taking page screenshot
    - disabled all CSS animations
  - waiting for fonts to load...
  - fonts loaded
  - 864 pixels (ratio 0.01 of all image pixels) are different.
  - waiting 100ms before taking screenshot
  - taking page screenshot
    - disabled all CSS animations
  - waiting for fonts to load...
  - fonts loaded
  - captured a stable screenshot
  - 864 pixels (ratio 0.01 of all image pixels) are different.

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - heading "File Upload" [level=1] [ref=e4]
  - generic [ref=e6] [cursor=pointer]:
    - button "file upload"
    - paragraph [ref=e7]: Drag & drop a file here, or click to select
```

# Test source

```ts
  1  | import { expect, test, openWithFonts, waitForFonts } from './fixtures';
  2  | 
  3  | const VIEWPORTS = [
  4  |   { name: 'desktop', width: 1280, height: 800 },
  5  |   { name: 'mobile', width: 390, height: 844 },
  6  | ] as const;
  7  | 
  8  | for (const viewport of VIEWPORTS) {
  9  |   test(`home page ${viewport.name}`, async ({ page }, testInfo) => {
  10 |     const brand = testInfo.project.name;
  11 |     await page.setViewportSize({ width: viewport.width, height: viewport.height });
  12 |     await openWithFonts(page, `/web/${brand}/`);
  13 |     await page.locator('body').waitFor({ state: 'attached' });
  14 |     await waitForFonts(page);
> 15 |     await expect(page).toHaveScreenshot(`home-${viewport.name}.png`, {
     |                        ^ Error: expect(page).toHaveScreenshot(expected) failed
  16 |       animations: 'disabled',
  17 |       maxDiffPixelRatio: 0.001,
  18 |     });
  19 |   });
  20 | }
  21 | 
```